/**
 * Class Name: Character
 * 
 * Purpose:
 * Holds the varaibles of the player and enemy characters
 * 
 * Last Revision: 2015-12-31
 * 
 */

public class Character
{
	final static int MIN_SCORE_VALUE = 0,
	         		 MAX_SCORE_VALUE = 5,
	         		 MAX_STAMINA = 30;
	
	int maxStamina = MAX_STAMINA, 
		stamina = maxStamina,
		offenseScore = 0,
		defenseScore = 0,
		toHitScore = 0;
	
	boolean isWounded = false;
	
	/**
	 * Method Name: consumeCurrentActionStamina()
	 * 
	 * Purpose:
	 * Instance method that adjusts the current STA value based on the
	 * current action's Score Values
	 * 
	 * Accepts: Nothing
	 * 
	 * Returns: Nothing
	 * 
	 */
	
	public void consumeCurrentActionStamina()
	{
		stamina -=(offenseScore + defenseScore);
		
	}// end void method consumeCurrentActionStamina
	
	/**
	 * Method Name: setStaminaToMax()
	 * 
	 * Purpose:
	 * Instance method that sets the current stamina value back to the max value
	 * of the character
	 * 
	 * Accepts: Nothing
	 * 
	 * Returns: Nothing
	 * 
	 */
	
	public void setStaminaToMax()
	{
		stamina = maxStamina;
		
	}// end void method setStaminaToMax	
	
	
}//end Character class
